#include <vector>
using namespace std;
class Solution {
    public:
        vector<TreeNode*> generateTrees(int n) {
            for ()

        }
};

int main(void) {
    Solution sl;
    vector<TreeNode*> res = sl.generateTrees(3);
}
